using WeifenLuo.WinFormsUI.Docking;

static void Main(IYnote ynote)
{
    var dlg = new DockContent();
    dlg.Text = "Test document";
    // use DockState.DockLeft, DockState.DockRight etc.
    dlg.Show(ynote.Panel, DockState.Document);
}